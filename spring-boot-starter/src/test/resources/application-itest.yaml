spring:
  application:
    name: ITest
  main:
    banner-mode: off
  jpa:
    open-in-view: true
    database: postgresql
    generate-ddl: true

feign:
  client:
    config:
      default:
        loggerLevel: BASIC
